---
entry_point: openspec-brainstorm
generation_mode: FULL
completed_stages: [INITIALIZED, DESIGN_READY, SPECS_DONE, TASKS_DONE, REVIEW_SATURATED]
---

# Brainstorm Session State
- **Current Phase**: IMPLEMENTATION COMPLETE (Revision 5 applied)
- **Pending Items**: Run `/opsx-verify` after manual QA

## Understanding Lock
- Status: CONFIRMED
- Confirmed at: 2026-06-24T13:51
- Summary: Thêm trường `type` (null | "new-word") vào Flashcard model; xây dựng study mode "Draw" mới dành cho CJK characters với canvas vẽ tay và so sánh nét vẽ với mẫu chuẩn. Người dùng đánh dấu type thủ công khi tạo/sửa thẻ.

## Working Notes (Scratchpad)
- Tech stack: Next.js 15 App Router, Prisma (PostgreSQL), TypeScript, Tailwind, shadcn/ui
- Existing study modes: Flashcard, Learn, Write (textarea), Test, Spaced Repetition
- WriteMode đã có dùng textarea + fuzzyMatch → Draw Mode sẽ dùng canvas thay thế
- DB: Flashcard model cần thêm field `type String? @default(null)` (hoặc enum)
- Scoring approach: So sánh nét vẽ với mẫu chuẩn → cần research thuật toán (hanzi-writer, $1 recognizer, DTW, hay pixel-diff)
- CJK focus: Hán tự (Traditional/Simplified), Kana Nhật, Hangul Hàn
- StudyLauncher cần hiển thị mode Draw, disabled nếu set không có thẻ type=new-word
- Mode Draw lưu kết quả vào study session giống các mode khác
